# Finance App

## Estrutura
- `backend/` — Flask API (Pedro)
- `frontend/` — HTML/CSS/JS (Laís)
- `ia/` — Camada de IA com Claude (Victor)

## Variáveis de ambiente
Copie `.env.example` para `.env` e preencha as chaves.
